services:
  laspoly-server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "${LASPOLY_PORT:-8080}:8080"
    environment:
      LASPOLY_BASE_URI: http://0.0.0.0:8080/laspoly/
    restart: unless-stopped
